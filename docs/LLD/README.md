# Low-Level Design

LLDs describe implementation-level design for approved components.

They should cover modules, interfaces, data models, configuration, error
handling, state transitions, testing, and operational behavior.

Do not create an LLD before the relevant HLD is reviewed.
